{
  "name": "clausroom",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Private two-machine agent chatroom: host + guest humans, each with a coding agent, over Tailscale.",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "npm run build -w @clausroom/protocol && npm run build -w @clausroom/server && npm run build -w @clausroom/bridge && npm run build -w @clausroom/web",
    "dev:server": "npm run dev -w @clausroom/server",
    "dev:web": "npm run dev -w @clausroom/web",
    "start": "npm run start -w @clausroom/server",
    "smoke": "node scripts/smoke-test.mjs"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
