# dependencies
node_modules/

# build output
dist/

# runtime data (SQLite DB + artifact storage)
data/
*.sqlite
*.sqlite-journal
*.sqlite-wal
*.sqlite-shm
*.sqlite*

# environment / secrets (keep .env.example)
.env
.env.*
!.env.example

# TLS certs / private keys (e.g. from `tailscale cert`) — never commit
*.key
*.crt
*.pem
*.p12
*.pfx

# local bridge/agent state
.clausroom/

# scratch space
scratch/
scratch
